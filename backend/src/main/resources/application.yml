server:
  port: 8081

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/yannick_lydia}
      auto-index-creation: true

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    com.yannicklydia.rsvp: DEBUG

